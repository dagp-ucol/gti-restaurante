<h1 class="my-3">Employee Index</h1>

<p>
  <%= link_to 'Add new employee', new_employee_path, class: 'btn btn-primary my-3' %>
</p>

<div class="card">
  <div class="card-body">
    <table class="table table-striped">
      <thead>
        <tr>
          <th scope="col">Id</th>
          <th scope="col">First name</th>
          <th scope="col">Last name</th>
          <th scope="col">Email</th>
          <th scope="col">Position</th>
          <th scope="col">Private number</th>
          <th scope="col">Working</th>
          <th scope="col">Store ID</th>
          <th coolspan=2>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @employees.each do |employee| %>
           <tr>
            <th scope="row"><%= employee.id %></th>
            <td><%= employee.first_name %></td>
            <td><%= employee.last_name %></td>
            <td><%= employee.email %></td>
            <td><%= employee.position %></td>
            <td><%= employee.private_number %></td>
            <td><%= employee.working %></td>
            <td><%= employee.store_id %></td>
            <td><%= link_to 'Show', employee_path(employee.id), class: 'btn btn-primary' %></td>
            <td><%= link_to 'Edit', edit_employee_path(employee.id), class: 'btn btn-success' %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
