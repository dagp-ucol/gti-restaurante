<%= form_with(model: inactive_ticket) do |form| %>
  <% if inactive_ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inactive_ticket.errors.count, "error") %> prohibited this inactive_ticket from being saved:</h2>

      <ul>
        <% inactive_ticket.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
